export type SatelliteMeta = {
  name: string;
  noradId: string;
  updatedAt: string;
};

export type TLE = {
  name: string;
  noradId: string;
  line1: string;
  line2: string;
  updatedAt: string;
};
